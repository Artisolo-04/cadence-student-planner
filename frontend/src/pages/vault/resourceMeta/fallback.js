import { Globe2, File as FileIcon } from "lucide-react";

export const LINK_FALLBACK = { kind: "url", badge: "LINK", Icon: Globe2, accentVar: "--color-primary" };
export const FILE_FALLBACK = { kind: "generic", Icon: FileIcon, accentVar: "--color-text-muted" };
